<div class='full_blog'>
  <h2><%= blog.title %> </h2>
  <h5><%= blog.headline %></h5>
  <h5><%= blog.user.name %> - Posted <%= minutes_in_words(blog.created_at) %> </h5>
  <p><%= blog.content %> </p>
</div>
